{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "build:shared": "lerna run build --scope=shared",
    "build:web": "lerna run build --scope=web",
    "dev:web": "lerna run dev --scope=web",
    "dev:shared": "lerna run start --scope=shared",
    "dev:mobile": "lerna run start --scope=mobile"
  },
  "devDependencies": {
    "lerna": "^7.2.0"
  }
}
