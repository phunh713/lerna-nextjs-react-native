{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "build:common": "lerna run build --scope=common",
    "build:web": "lerna run build --scope=web",
    "dev:web": "lerna run dev --scope=web",
    "dev:common": "lerna run start --scope=common",
    "dev:mobile": "lerna run start --scope=ExampleApp"
  },
  "devDependencies": {
    "lerna": "^7.2.0"
  }
}
